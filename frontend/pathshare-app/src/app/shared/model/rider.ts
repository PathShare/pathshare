/**
 * @description Displays user details on user interfaces. Similar to the User
 * class, but does not include private fields such as usernames or passwords.
 */
export class Rider {
    id: number;
    name: string;
    major: string;
    age: number;
}
